language: python

matrix:
  include:
    - python: '2.7'
      env: TEST_SUITE=python2
    - python: '3.6'
      env:
        - TEST_SUITE=python3
        - TYPE_CHECK=1
    - python: "nightly"
      env: TEST_SUITE=python3

install:
  - pip install --user types
  - pip3 install --user mypy

script:
  - ./run_tests.sh

